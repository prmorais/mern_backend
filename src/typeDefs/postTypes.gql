
type Post {
  id: ID!
  title: String!
  description: String!
}

type Query {
  totalPost: Int!
  allPosts: [Post!]!
}

# Mutations
type Mutation {
  newPost(title: String!, description: String!): Post!
}